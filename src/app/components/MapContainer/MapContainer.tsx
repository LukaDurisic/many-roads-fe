import React from "react";
import styles from "./MapContainer.module.css";

function MapContainer() {
  return <div className={styles.temp}>Map container is working</div>;
}

export default MapContainer;
